// const navs = document.querySelectorAll(".nav-item");

// function selectPage() {
//   if (navs) {
//     navs.forEach((item) => item.classList.remove("selected"));
//     this.classList.add("selected");
//   }
// }

// navs.forEach((item) => item.addEventListener("click", selectPage));

// currentPath = window.location.pathname || "/";
// selectedNav = document.querySelector(
//   `.navbar .nav-link[href*="${currentPath}"]:not(.logo-link)`
// );
// console.log(selectedNav);
// selectedNav.closest(".nav-item").classList.add("selected");

